#include "main.h"
/**
 * _abs - check number
 * @i: the signed number
 * Description: 'prints _putchar too stdout'
 * Return: always 0
 */
int _abs(int i);
{
	if (n > 0)
	{
		return (n);
	}
	if (n == 0)
	{
		return (0);
	}
	else
	{
		return (n * (-1));
	}
	_putchar('\n');
}
